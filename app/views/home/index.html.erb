<div class="viewer">
    <!-- Slider main container -->
    <div class="swiper-container">
        <!-- Additional required wrapper -->
        <div class="swiper-wrapper">
            <!-- Slides -->
            <div class="swiper-slide index1">
                <div>
                    <button type="button" class="btn btn-primary centered5 labelfont3 btn1">회원가입</button>
                </div>
                <div>
                    <button type="button" class="btn btn-primary centered4 labelfont3 btn1">로그인</button>
                </div>
            </div>
            <div class="swiper-slide index1">
            </div>
            <div class="swiper-slide index3">
                Slide 3
            </div>
        </div>
        <!-- If we need pagination -->
        <div class="swiper-pagination"></div>

        <!-- If we need navigation buttons -->
        <%# <div class="swiper-button-prev"></div>
        <div class="swiper-button-next"></div> %>

        <!-- If we need scrollbar -->
        <div class="swiper-scrollbar"></div>
    </div>
</div>
